{
  "name": "jltquest-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=web",
    "dev:no-mobile": "turbo dev --filter=!mobile",
    "tunnel": "cloudflared tunnel --url http://localhost:3000",
    "tunnel:web": "cloudflared tunnel --url http://localhost:3000",
    "postinstall": "pnpm --filter @jlt/database db:generate",
    "db:generate": "pnpm --filter @jlt/database db:generate",
    "db:push": "pnpm --filter @jlt/database db:push",
    "db:seed": "pnpm --filter @jlt/database db:seed",
    "setup": "pnpm install && pnpm db:generate && pnpm db:push && pnpm db:seed",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "eslint": "^9.20.0",
    "prettier": "^3.5.0",
    "sharp": "^0.35.3",
    "turbo": "^2.5.0",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "^19.0.0",
      "@types/react-dom": "^19.0.0"
    }
  }
}
